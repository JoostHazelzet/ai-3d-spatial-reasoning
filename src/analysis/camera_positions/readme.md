# Camera positions

Shows the camera positions as defined by the MathPlotLib view from `analysis/3d-orthographics/generate_demo.ipynb`.

To run:

```sh
python -m http.server 8002
```

Open in browswer:

http://localhost:8002/voxel_viewer.html